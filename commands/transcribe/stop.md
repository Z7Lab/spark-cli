# transcribe stop

```spec
{
  "name": "transcribe.stop",
  "domain": "transcribe",
  "subcommand": "stop",
  "summary": "Stop the running whisper-server",
  "handler": "transcribe.stop",
  "params": []
}
```

Stops the running whisper-server. No-op (with a note) if it isn't running.
